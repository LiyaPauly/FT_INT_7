//Add the string “1” and the digit 2 (both operands must be in variables), and get a mathematically correct answer.

let str = "1";
let num = 2;
// Convert the string to a number and add
let Output = Number(str) + num;

console.log(`The output is :`, Output); // Output: 3